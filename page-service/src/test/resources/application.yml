spring:
  boot:
    admin:
      client:
        enabled: false

  cloud:
    config:
      enabled: false

eureka:
  client:
    enabled: false

gorvat:
  services:
    book:
      uri: http://localhost:54321